# Chromium Testdata

Rust unit tests build deterministic SQLite fixtures at runtime. This directory is reserved for larger checked-in fixtures once licensing and provenance are documented.

The synthetic SQL source used by examples lives in `datasets/samples/chromium/History.sql`.
