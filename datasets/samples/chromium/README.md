# Synthetic Chromium History Dataset

`History.sql` defines a minimal Chromium-family History SQLite database for parser development and documentation examples.

The generated `History` database is synthetic. It does not contain real user browsing data.

Regenerate:

```bash
scripts/make_sample_datasets.sh
```
